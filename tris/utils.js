/** @format */

// swaps elements of 'values' at indices 'i' and 'j'
function swap(i, j) {
  // adjust the pace of the simulation by changing the
  // value
  let temp = arr[i];
  arr[i] = arr[j];
  arr[j] = temp;
}
